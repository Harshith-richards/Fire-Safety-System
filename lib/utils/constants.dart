import 'package:flutter/material.dart';

// --- THEME COLORS ---
const Color kPrimaryColor = Color(0xFF1A1A2E);
const Color kSecondaryColor = Color(0xFF16213E);
const Color kAccentColor = Color(0xFFE94560);
const Color kTextColor = Colors.white;
const Color kGlassColor = Colors.white10;

// --- CONFIG ---
// Update this with your server IP
const String baseUrl = ' '; // <- CHANGE THIS to local ip address, if you are using the same network as the ESP32, or else you can host in cloudflare and host theri and replace it with cloudflare url 
