import 'package:flutter/material.dart';
import 'package:animate_do/animate_do.dart';

// Re-exporting animate_do widgets for convenience or wrapping them if needed.
// For now, we can just use animate_do directly in screens, but if we want to centralize:

export 'package:animate_do/animate_do.dart';
